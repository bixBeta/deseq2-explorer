{
  "name": "deseq2-notify-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker notification relay for DESeq2 ExploreR",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev":    "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
