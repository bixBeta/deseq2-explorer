name             = "deseq2-notify"
main             = "worker.js"
compatibility_date = "2025-01-01"

# KV namespace for per-recipient rate limiting.
# Create it once with:  npx wrangler kv:namespace create RATE_KV
# Then paste the returned id below.
[[kv_namespaces]]
binding    = "RATE_KV"
id         = "REPLACE_WITH_KV_NAMESPACE_ID"
